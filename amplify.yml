version: 1
frontend:
    phases:
        build:
            commands:
                - yarn --force
                - yarn build
    artifacts:
        baseDirectory: out
        files:
            - '**/*'
    cache:
        paths:
            - node_modules/**/*
