version: 1
frontend:
    phases:
        build:
            commands:
                - yarn --force
                - yarn build
    artifacts:
        baseDirectory: .next
        files:
            - '**/*'
    cache:
        paths:
            - node_modules/**/*
