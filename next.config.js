const config = {
    // rewrites: () => [STUDIO_REWRITE],
    i18n: {
        locales: ['en-SG', 'zh-SG'],
        defaultLocale: 'en-SG',
    },
    compiler: {
        styledComponents: true,
    },
    reactStrictMode: true,
}
module.exports = config
